{
  "projects": [
    {
      "src": "client",
      "use": "@verceljs/node"
    },
    {
      "src": "server",
      "use": "@verceljs/node"
    }
  ],
  "buildCommand": "cd client && npm run build",
  "outputDirectory": "client/dist",
  "env": {
    "VITE_API_URL": "@api_url"
  }
}
